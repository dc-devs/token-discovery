{
	"name": "token-discovery",
	"author": "David Christian <david.w.christian@gmail.com>",
	"license": "ISC",
	"description": "",
	"version": "1.0.0",
	"main": "dist/index.js",
	"private": true,
	"scripts": {
		"test": "lerna run test",
		"build": "lerna run build",
		"build:clean": "yarn reset && yarn && yarn build",
		"reset": "lerna run reset && nx reset",
		"watch": "lerna watch -- lerna run build",
		"graph": "nx graph --file=output.json"
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"lerna": "^8.0.0"
	}
}
